# function fish_right_prompt --description 'Write out the prompt on the right side (starship style)'
#     set -l exit_code $status
#     # Account for changes in variable name between v2.7 and v3.0
#     set -l starship_duration "$CMD_DURATION$cmd_duration"
#     set --export STARSHIP_CONFIG "/home/cosmicdoge/.config/starship-right.toml"
#     "/home/cosmicdoge/.cargo/bin/starship" prompt --status=$exit_code --cmd-duration=$starship_duration --jobs=(count (jobs -p))
# end
