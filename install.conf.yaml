- defaults:
    link:
      relink: true
      create: true

- clean:
    ~/:
    ~/.config:
      recursive: true

- link:
    ~/:
      glob: true
      path: home/*
      prefix: '.'

    ~/.config/fish/:
      path: shell/fish/**
      glob: true

  #    ~/.config/systemd/user:

    ~/.config/doom:

    #    ~/.config/starship.toml: shell/starship.toml
