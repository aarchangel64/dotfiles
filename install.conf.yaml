- defaults:
    link:
      relink: true
      create: true

- clean:
    ~/:
    ~/.config:
      recursive: true

- link:
    ~/.config/fish/:
      path: shell/fish/**
      glob: true

    ~/.config/doom:

    #    ~/.config/starship.toml: shell/starship.toml
