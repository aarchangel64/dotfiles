- defaults:
    link:
      relink: true
      create: true

- clean:
    ~/:
    ~/.config:
      recursive: true

- link:
    ~/:
      glob: true
      path: home/*
      prefix: '.'
 
    ~/.config/:
      glob: true
      path: config/**

    ~/.config/fish/:
      glob: true
      path: shell/fish/**

    ~/.config/doom:

  #    ~/.config/systemd/user:


    #    ~/.config/starship.toml: shell/starship.toml
