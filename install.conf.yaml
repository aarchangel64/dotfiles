- defaults:
    link:
      relink: true
      create: true

- clean:
    ~/:
    ~/.config:
      recursive: true

- link:
    ~/.gitconfig:
    ~/.tmux.conf:
    ~/.config/picom.conf:
    ~/.Xresources:
    ~/.xprofile:
    ~/.config/awesome:
    ~/.config/kitty:
    ~/.config/qmk:
    ~/.config/doom:
    ~/.config/pulse:
    ~/.config/chemacs:

    ~/.config/fish/functions:
      path: fish/functions/*.fish
      glob: true

    ~/.config/fish/:
      path: fish/*
      glob: true
